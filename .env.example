# Copy to .env and fill in. .env itself is gitignored.

DATABASE_URL="file:./dev.db"

# Session signing secret for express-session (httpOnly cookies).
SESSION_SECRET="generate-a-real-random-value"

# SMTP notification sender (nodemailer).
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
